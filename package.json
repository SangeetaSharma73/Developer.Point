{
  "name": "developer.point",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace to manage frontend and backend",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run server --prefix backend\"",
    "install:all": "npm install",
    "build:all": "npm run build --prefix frontend && npm run build --prefix backend",
    "f:add": "npm install -w frontend",
    "b:add": "npm install -w backend",
    "f:remove": "npm uninstall -w frontend",
    "b:remove": "npm uninstall -w backend",
    "f": "npm run -w frontend",
    "b": "npm run -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
